{
  "entities": {
    "Enquiry": {
      "title": "Enquiry",
      "description": "User enquiry from the contact form",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "club": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "message": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "email", "message", "createdAt"]
    },
    "Registration": {
      "title": "Registration",
      "description": "User registration for a run event",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "gender": { "type": "string" },
        "event": { "type": "string" },
        "transactionId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "registeredAt": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["pending", "verified"] }
      },
      "required": ["name", "email", "phone", "gender", "event", "transactionId", "createdAt", "registeredAt", "status"]
    }
  },
  "firestore": {
    "/enquiries/{enquiryId}": {
      "schema": "Enquiry",
      "description": "Collection of user enquiries"
    },
    "/registrations/{registrationId}": {
      "schema": "Registration",
      "description": "Collection of event registrations"
    },
    "/events_2025_new/{registrationId}": {
      "schema": "Registration",
      "description": "New collection for event registrations starting May 2025"
    },
    "/event_starbucks/{registrationId}": {
      "schema": "Registration",
      "description": "New collection for Starbucks Grande Run event registrations"
    },
    "/event_murphies_sunday_reset/{registrationId}": {
      "schema": "Registration",
      "description": "New collection for Murphies Sunday Reset event registrations"
    },
    "/events_2026_new/{registrationId}": {
      "schema": "Registration",
      "description": "New collection for event registrations starting June 2026"
    },
    "/event_third_wave/{registrationId}": {
      "schema": "Registration",
      "description": "New collection for Third Wave Sunday Run event registrations"
    },
    "/event_kuti/{registrationId}": {
      "schema": "Registration",
      "description": "New collection for Kuti Coffee Run event registrations"
    }
  }
}
